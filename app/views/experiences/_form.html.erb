<%= form_with(model: experience, local: true) do |form| %>
  <% if experience.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(experience.errors.count, "error") %> prohibited this experience from being saved:</h2>

      <ul>
      <% experience.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :company %>
    <%= form.text_field :company, id: :experience_company %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from, id: :experience_from %>
  </div>

  <div class="field">
    <%= form.label :to %>
    <%= form.text_field :to, id: :experience_to %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.text_area :role, id: :experience_role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
